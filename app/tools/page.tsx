'use client';
import {useState} from 'react';import Header from '@/components/Header';import CurrencyCard from '@/components/CurrencyCard';import LocalTimeCard from '@/components/LocalTimeCard';import {tripDays} from '@/data/trip';
export default function Tools(){const [id,setId]=useState('2026-11-20');const d=tripDays.find(x=>x.id===id)!;return <><Header subtitle="Currency · time · phrases · emergency"/><div className="card"><label className="eyebrow" htmlFor="toolDay">DESTINATION</label><select id="toolDay" className="input" value={id} onChange={e=>setId(e.target.value)} style={{marginTop:8}}>{tripDays.map(x=><option key={x.id} value={x.id}>{x.label} · {x.place}</option>)}</select></div><div className="spacer"/><div className="grid grid2"><CurrencyCard currency={d.currency}/><LocalTimeCard timezone={d.timezone}/></div><div className="spacer"/><div className="grid grid2"><div className="card"><div className="eyebrow">TIPPING</div><div className="muted" style={{marginTop:8}}>{d.tipping}</div></div><div className="card"><div className="eyebrow">EMERGENCY</div><div className="metric smallMetric">{d.emergency}</div></div></div><div className="spacer"/><div className="card"><div className="eyebrow">USEFUL PHRASE</div><div className="metric smallMetric">{d.phrase[0]||'—'}</div><div className="muted">{d.phrase[1]}</div></div></>}
