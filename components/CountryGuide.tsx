import {getCountryGuide} from '@/data/countryGuide';
export default function CountryGuide({country}:{country:string}){const g=getCountryGuide(country);return <div className={`card countryGuide ${g.theme}`}><div className="countryGuideHead"><div><div className="eyebrow">YOU ARE IN</div><div className="countryName"><span aria-hidden>{g.flag}</span><strong>{g.name}</strong></div></div><span className="pill">{g.risk}</span></div><div className="countryGuideGrid"><div><div className="eyebrow">SAFE TRAVEL</div>{g.safety.map((x,i)=><div className="countryTip" key={i}>• {x}</div>)}</div><div><div className="eyebrow">CUSTOMS & ETIQUETTE</div>{g.customs.map((x,i)=><div className="countryTip" key={i}>• {x}</div>)}</div></div>{g.source&&<div className="actionRow" style={{marginTop:12}}><a className="btn" href={g.source} target="_blank" rel="noreferrer">Check current Government of Canada advisory</a></div>}</div>}
