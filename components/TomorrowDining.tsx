'use client';
import {DiningMenu} from '@/data/diningMenus';
export default function TomorrowDining({dayLabel,menus}:{dayLabel:string;menus:DiningMenu[]}){if(!menus.length)return null;return <div className="card"><div className="eyebrow">TOMORROW NIGHT · MENU PREVIEW</div><h2 className="sectionTitle" style={{marginTop:6}}>{dayLabel}</h2><div className="muted small">Review dinner choices tonight. Menus below are official Oceania samples and can change onboard.</div><div className="spacer"/>{menus.map(m=><div className="soft" key={`${m.date}-${m.restaurant}`} style={{marginTop:8}}><div className="portCard"><div><strong>{m.restaurant}</strong><div className="muted small">{m.time||'Dinner'} · {m.cuisine}</div></div><span className="pill">MENU</span></div><p className="muted small">{m.summary}</p><div className="actionRow">{m.highlights.map(x=><span className="chip" key={x}>{x}</span>)}</div><div className="actionRow" style={{marginTop:10}}><a className="btn primary" href={m.menuUrl} target="_blank" rel="noreferrer">View official menu</a></div><div className="muted small" style={{marginTop:8}}>{m.note}</div></div>)}</div>}
