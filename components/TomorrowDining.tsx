'use client';
import {DiningMenu} from '@/data/diningMenus';
export default function TomorrowDining({dayLabel,menus,heading='DINING MENU PREVIEW',intro='Review the menu before dinner.'}:{dayLabel:string;menus:DiningMenu[];heading?:string;intro?:string}){if(!menus.length)return null;return <div className="card"><div className="eyebrow">{heading}</div><h2 className="sectionTitle" style={{marginTop:6}}>{dayLabel}</h2><div className="muted small">{intro} Official Oceania sample menus are shown below and can change onboard.</div><div className="spacer"/>{menus.map(m=><div className="soft" key={`${m.date}-${m.restaurant}`} style={{marginTop:8}}><div className="portCard"><div><strong>{m.restaurant}</strong><div className="muted small">{m.time||'Dinner'} · {m.cuisine}</div></div><span className="pill">MENU</span></div><p className="muted small">{m.summary}</p><div className="actionRow">{m.highlights.map(x=><span className="chip" key={x}>{x}</span>)}</div><div className="actionRow" style={{marginTop:10}}><a className="btn primary" href={m.menuUrl} target="_blank" rel="noreferrer">Open full official menu</a></div><div className="muted small" style={{marginTop:8}}>{m.note}</div></div>)}</div>}
