import Link from 'next/link';import Header from '@/components/Header';import {tripDays} from '@/data/trip';
export default function Trip(){return <><Header subtitle="Full Nov 12–26 itinerary"/><div className="timeline">{tripDays.map((d,i)=><Link className="card timelineCard" href={`/day/${d.id}`} key={d.id}><div className="timelineMarker"><span>{i+1}</span></div><div><div className="eyebrow">{d.label.toUpperCase()}</div><h2 className="sectionTitle" style={{marginTop:6}}>{d.place}</h2><div className="muted">{d.country} · {d.currency}</div><div className="actionRow" style={{marginTop:10}}>{d.shipDeparture&&<span className="pill">Ship departs {d.shipDeparture}</span>}{d.walkSummary&&<span className="pill">{d.walkSummary}</span>}</div><div className="row"><strong>{d.events[0]?.time||'Plan'}</strong><div className="muted">{d.events[0]?.title}</div></div></div></Link>)}</div></>}
